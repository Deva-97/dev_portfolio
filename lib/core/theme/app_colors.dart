import 'package:flutter/material.dart';

class AppColors {
  // Modern blue gradient colors
  static const primary = Color(0xFF3B82F6);
  static const primaryDark = Color(0xFF1E40AF);
  static const primaryLight = Color(0xFF60A5FA);

  // Neutral colors
  static const secondary = Color(0xFF1F2937);
  static const tertiary = Color(0xFF374151);
  static const muted = Color(0xFF6B7280);
  static const lightText = Color(0xFF6B7280);

  // Background colors
  static const lightBg = Color(0xFFF9FAFB);
  static const darkBg = Color(0xFF0F172A);
  static const cardBg = Color(0xFFFAFAFA);
  static const darkCardBg = Color(0xFF1E293B);

  // Text colors for light mode brightness
  static const lightModeText = Color(0xFF1F2937);
  static const lightModeHint = Color(0xFF4B5563);

  // Accent colors (removed pink, using cyan instead)
  static const accent = Color(0xFF06B6D4);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
}
